<odoo>
  <data>

        <record id="purchase_order_view_inherit_tree" model="ir.ui.view">
            <field name="name">purchase.order.view.tree</field>
            <field name="model">purchase.order</field>
            <field name="arch" type="xml">
                <list
                    decoration-muted="state=='cancel'"
                    decoration-info="state in ('wait','confirmed')"
                    string="Purchase Order"
                    class="o_purchase_order"
                    sample="1" create="0" edit="0">
                    <header>
                        <button name="action_create_invoice" type="object" string="Create Bills"/>
                    </header>
                    <field name="priority" optional="show" widget="priority" nolabel="1"/>
                    <field name="partner_ref" optional="hide"/>
                    <field name="name" string="Reference" readonly="1" decoration-bf="1"/>
                    <field name="date_approve" widget="date" invisible="context.get('quotation_only', False)" optional="show"/>
                    <field name="partner_id"/>
                    <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" optional="show"/>
                    <field name="date_planned" invisible="context.get('quotation_only', False)" optional="hide"/>
                    <field name="date_order" invisible="not context.get('quotation_only', False)" optional="hide"/>
                    <field name="request_id" optional="show"/>
                     <field name="department_id" optional="show"/>

                    <field name="amount_total" sum="Total amount" widget="monetary" optional="show" decoration-bf="1"/>
                    <field name="currency_id"  optional="show"/>
                        <field name="state" optional="hide" invisible="1"/>

                    <field name="invoice_status" widget="badge" decoration-success="invoice_status == 'invoiced'" decoration-info="invoice_status == 'to invoice'" optional="show"/>
                    <field name="payment_state" widget="badge" optional="show"/>

                    

                <field name="delivery_status" widget="badge" optional="show"/>



                </list>
            </field>
        </record>    


    <record id="purchase_form_inherit_action" model="ir.actions.act_window">
            <field name="name">Purchase Orders</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">purchase.order</field>
            <field name="view_mode">list,form</field>
            <field name="view_id" ref="purchase_order_view_inherit_tree"/>
            <field name="domain">[('state','=','purchase'),('ore_purchased','=',False),('contract_purchased','=',True)]</field>
            <field name="search_view_id" ref="purchase.purchase_order_view_search"/>
            <field name="context">{}</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                No purchase order found. Let's create one!
              </p><p>
                Once you ordered your products to your supplier, confirm your request for quotation and it will turn into a purchase order.
              </p>
            </field>
        </record>



  <menuitem action="purchase_form_inherit_action" id="menu_purchase_read_form_action"   parent="material_request.menu_material_request_main"/>





 <record id="purchase_order_inherited_advance_payments_form_view" model="ir.ui.view">
        <field name="name">purchase.order.inherited.advance.payment.form.view</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
           
            <xpath expr="//notebook" position="inside">
                <page string="Account Payment" groups="account.group_account_user,purchase.group_purchase_user">
                    <field name="payment_line" mode="list" readonly="1">
                        <list editable="bottom">
                            <field name="purchase_id" invisible="1"/>
                            <field name="memo"/>
                            <field name="partner_id"/>
                            <field name="date"/>
                            <field name="create_uid"/>
                            <field name="create_date"/>
                            <field name="state" invisible="1"/>
                            <field name="journal_id"/>
                            <field name="payment_method_id"/>
                            <field name="amount" sum="amount"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
  </data>
</odoo>