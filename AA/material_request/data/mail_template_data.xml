<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

      <!--   <record id="email_template_edi_material_request_reminder" model="mail.template">
            <field name="name">Material Request: mail Reminder</field>
            <field name="model_id" ref="material_request.model_material_request"/>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) |safe}</field>
            <field name="subject">${object.company_id.name} Order (Ref ${object.name or 'n/a' })</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="body_html" type="html">
        <div style="margin: 0px; padding: 0px;">
        <p style="margin: 0px; padding: 0px; font-size: 13px;">
        Dear ${object.partner_id.name}
        % if object.partner_id.parent_id:
            (${object.partner_id.parent_id.name})
        % endif
        <br/><br/>
        Here is a reminder that the delivery of the purchase order <strong>${object.name}</strong>
        % if object.partner_ref:
            <strong>(${object.partner_ref})</strong>
        % endif 
        is expected for 
        % if object.date_planned:
            <strong>${format_date(object.date_planned)}</strong>.
        % else:
            <strong>undefined</strong>.
        % endif
        Could you please confirm it will be delivered on time?
        </p>
        </div></field> -->
            <!-- <field name="report_template" ref="action_report_purchase_order"/>
            <field name="report_name">PO_${(object.name or '').replace('/','_')}</field> -->
          <!--   <field name="lang">${object.partner_id.lang}</field>
            <field name="auto_delete" eval="True"/>
        </record> -->

   <record id="seq_purchase_tender1" model="ir.sequence">
            <field name="name">Call for Tender</field>
            <field name="code">purchase.contract.purchase.tender</field>
            <field name="prefix">TE</field>
            <field name="padding">5</field>
            <field name="company_id" eval="False"></field>
        </record>

        <record id="seq_blanket_order1" model="ir.sequence">
            <field name="name">Contract</field>
            <field name="code">purchase.contract.blanket.order</field>
            <field name="prefix">CT</field>
            <field name="padding">5</field>
            <field name="company_id" eval="False"></field>
        </record>




        <!-- Approval activities -->
        <record id="mail_act_material_request_approval" model="mail.activity.type">
            <field name="name">Action Required</field>
            <field name="icon">fa-check</field>
        </record>

    <record id="material_inspection" model="ir.sequence">
            <field name="name">Material Inspection</field>
            <field name="code">material.inspection</field>
            <field name="prefix">MI</field>
            <field name="padding">5</field>
            <field name="company_id" eval="False"/>
        </record>

   


    </data>
</odoo>
