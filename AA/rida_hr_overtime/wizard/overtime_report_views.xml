<!-- views/overtime_report_views.xml -->
<odoo>
  <data>
<!--     Action window for wizard (for menu) -->
  <!--   <record id="action_overtime_report_wizard" model="ir.actions.act_window">
      <field name="name">Overtime Excel Report</field>
      <field name="res_model">overtime.report.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
      <field name="view_id" ref="rida_hr_overtime.overtime_report_wizard_form_view"/>
    </record> -->

    <!-- Form view for wizard -->
    <record id="overtime_report_wizard_form_view" model="ir.ui.view">
      <field name="name">overtime.report.wizard.form</field>
      <field name="model">overtime.report.wizard</field>
      <field name="arch" type="xml">
        <form string="Overtime Report">
          <sheet>
            <group>
              <field name="company_id"/>
              <field name="month"/>
              <field name="year"/>
              <field name="department_id"/>
            </group>
            <footer>
              <button string="Export XLSX" type="object" name="action_print_xlsx" class="btn-primary"/>
              <button string="Cancel" class="btn-default" special="cancel"/>
            </footer>
          </sheet>
        </form>
      </field>
    </record>

    <!-- Menu item -->
    <menuitem id="menu_overtime_reports_root" name="Overtime Reports"
              parent="overtime_root_menu" sequence="95"/>

    <record id="action_act_overtime_reports" model="ir.actions.act_window">
      <field name="name">Overtime Reports</field>
      <field name="res_model">overtime.report.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>

    <menuitem id="menu_overtime_report_wizard" name="Export Overtime (XLSX)"
              parent="menu_overtime_reports_root" action="action_act_overtime_reports"/>

    <!-- Add button on hr.overtime.batch form to open wizard prefilled -->
    <!-- Replace with your original form view XML id -->
    
   <!--  <record id="view_hr_overtime_batch_form_inherit_report" model="ir.ui.view">
      <field name="name">hr.overtime.batch.form.inherit.report</field>
      <field name="model">hr.overtime.batch</field>
      <field name="inherit_id" ref="hr_overtime_request_form"/> 
      <field name="arch" type="xml">
        <xpath expr="//header" position="inside">
          <button name="%(action_overtime_report_wizard)d"
                  type="action"
                  string="Export Overtime"
                  class="oe_highlight"
                   invisible="state in ['draft','hr_off','w_hr_m','wfm','internal_aud','wd','posted']"/>
        </xpath>
      </field>
    </record> -->

  </data>
</odoo>
