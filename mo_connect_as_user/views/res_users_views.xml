<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Add "Connect As" button to the user form view -->
    <record id="view_users_form_connect_as" model="ir.ui.view">
        <field name="name">res.users.form.connect.as</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_connect_as"
                        type="object"
                        string="Connect As"
                        class="btn-primary"
                        icon="fa-sign-in"
                        groups="mo_connect_as_user.group_connect_as_user"/>
            </xpath>
        </field>
    </record>

</odoo>
