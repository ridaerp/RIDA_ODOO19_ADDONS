<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data>
        <!-- Add you code here -->
        <!-- model.name inherit form view -->
        <record id="stock_scrap_inherit_view" model="ir.ui.view">
            <field name="name">stock.scrap.inherit.view.form</field>
            <field name="model">stock.scrap</field>
            <field name="inherit_id" ref="stock.stock_scrap_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="replace">
                <header>
                    <button name="submit" invisible="state not in ('draft')" string="Submit" type="object" class="oe_highlight"/>
                    <button name="site_manager_approval"   invisible="state not in ('site_manager')" string="Operation Director Validate" type="object" class="oe_highlight"/>
                    <button name="action_validate"   invisible="state not in ('ccso_approval')" string="CCSO Validate" type="object" class="oe_highlight" context="{'not_unlink_on_discard': True}"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,done"/>
                </header>
            </xpath>
            </field>
        </record>   
    </data>
</odoo>